# Phase 9 — Settings Screen

## Objective
Build the full settings screen matching the Ord design layout, with theme persistence, notification toggle, scheduling context editing, feedback, and account info.

## Context
- See `../BRIEF.md`
- See `../ROADMAP.md`
- Design reference: `c:\Users\anhdu\Downloads\ord_settings.html`
- Existing settings screen: `src/app/(tabs)/settings.tsx` (sign out only)
- Existing BottomSheet primitive: `src/components/primitives/BottomSheet.tsx`
- Theme provider: `src/providers/theme-provider.tsx`
- User preferences type: `src/types/userPreferences.ts`
- Profile API placeholder: `src/features/profile/api.ts`
- Ord feedback templates: `C:\Ord\src\constants\feedback-templates.ts`

## Dependencies to Install
- `@react-native-async-storage/async-storage` — local theme persistence
- `expo-application` — app version display

## Tasks

### 1. Install dependencies
- `npx expo install @react-native-async-storage/async-storage expo-application`

### 2. Toast primitive — TESTS FIRST
- Write `src/components/primitives/__tests__/Toast.test.tsx`
  - renders message when `showToast()` called
  - auto-dismisses after 3s
  - can show multiple messages sequentially
- Then implement `src/components/primitives/Toast.tsx` + `useToast()` hook
- Provider at app root or inline — keep it simple

### 3. ListSelector primitive — TESTS FIRST
- Write `src/components/primitives/__tests__/ListSelector.test.tsx`
  - renders options with labels
  - shows checkmark on selected item
  - calls `onSelect` when row pressed
  - calls `onClose` when backdrop pressed
- Then implement `src/components/primitives/ListSelector.tsx`
- Full-width rows with label text and checkmark on selected
- Reuse BottomSheet internally

### 4. Profile API — TESTS FIRST
- Write `src/features/profile/__tests__/api.test.ts`
  - `fetchUserProfile()` returns user email/name from Supabase auth
  - `fetchUserProfile()` handles missing user
  - `fetchUserPreferences()` returns preferences row
  - `fetchUserPreferences()` handles missing row
  - `updateUserPreferences(partial)` calls Supabase update correctly
- Then implement `src/features/profile/api.ts`
  - `fetchUserProfile()` — get user from Supabase auth
  - `fetchUserPreferences()` — get preferences from `user_preferences` table
  - `updateUserPreferences(partial)` — update preferences

### 5. UserProfile hook — TESTS FIRST
- Write `src/features/profile/__tests__/useUserProfile.test.tsx`
  - returns loading state initially
  - returns user data after fetch
  - returns error on failure
  - derives initials from name/email
- Then implement `src/features/profile/hooks/useUserProfile.ts`

### 6. ThemeProvider update — TESTS FIRST
- Write `src/providers/__tests__/theme-provider.test.tsx`
  - reads preference from AsyncStorage on mount (default: 'system')
  - `setThemePreference()` writes to AsyncStorage and updates context
  - uses `useColorScheme()` when preference is 'system'
  - forces light/dark when preference is 'light'/'dark'
- Then update `src/providers/theme-provider.tsx`
  - Add `themePreference` state: `'system' | 'light' | 'dark'`
  - Read from AsyncStorage on mount
  - Expose `setThemePreference(preference)` via context
  - New `useThemePreference()` hook returns `{ preference, setThemePreference }`

### 7. Notification preference hook — TESTS FIRST
- Write `src/hooks/__tests__/useNotificationPreference.test.tsx`
  - reads initial state from AsyncStorage + OS permission status
  - toggle ON + permission not requested → calls `requestPermissionsAsync()`
  - toggle ON + permission denied → shows toast, reverts toggle
  - toggle ON + permission granted → stores preference
  - toggle OFF → stores preference, does not revoke OS permission
- Then implement `src/hooks/useNotificationPreference.ts`
- In-app preference stored in AsyncStorage
- On mount: query actual OS permission status via `getPermissionsAsync()`

### 8. SchedulingContextSheet — TESTS FIRST
- Write `src/features/profile/__tests__/SchedulingContextSheet.test.tsx`
  - renders wake/bed time pickers and text area
  - loads current values from preferences
  - calls `updateUserPreferences` on save
  - shows loading state during save
  - closes on save success
- Then implement `src/features/profile/components/SchedulingContextSheet.tsx`
- Reuse BottomSheet primitive
- Wake up time picker (DateTimePicker, mode='time')
- Bed time picker (DateTimePicker, mode='time')
- Freeform text area (TextInput multiline)

### 9. Feedback templates — TESTS FIRST
- Write `src/constants/__tests__/feedback-templates.test.ts`
  - templates have required fields (subject, body)
  - `buildMailtoUrl()` produces correct mailto URL
- Then create `src/constants/feedback-templates.ts`
- Same 3 templates as Ord (Bug report, Feature request, Other)
- Email: `anhducams@gmail.com`

### 10. Assemble Settings screen — TESTS FIRST
- Write `src/app/(tabs)/__tests__/settings.test.tsx`
  - renders all sections (Account, AI Preferences, Notifications, Preferences, About)
  - sign out calls `signOut()`
  - scheduling context row opens bottom sheet
  - theme row opens ListSelector
  - feedback row opens ListSelector
  - notification toggle reflects current state
  - version displays app version
- Then implement `src/app/(tabs)/settings.tsx`
- Sections in order: Account, AI Preferences, Notifications, Preferences, About, (spacer), Sign Out
- Section headers: uppercase label text (11px, tertiary color)
- Cards: white background, rounded corners, border, rows with icon + label + right element
- Scrollable full page with safe area insets
- Privacy policy row: exists, no-op for now

## Verification
- `npm test` — all new and existing tests pass
- App runs without errors
- Settings screen matches design layout (sections, cards, rows)
- Theme picker switches between system/light/dark, persists across app restarts
- Notification toggle requests permission on first enable
- Scheduling context sheet loads and saves wake/sleep times + freeform text
- Feedback opens mailto with correct template
- Version shows real app version
- Sign out works

## Done When
- Settings screen fully functional with all sections
- Theme persistence works locally
- Notification preference works with OS permission flow
- Scheduling context saves to Supabase
- Unit tests written and passing (TDD — tests before implementation)
- No TypeScript errors

## Notes
- AsyncStorage not yet installed — first task
- No toast library — build minimal one
- Subscription section intentionally omitted (no-op for now)
- Privacy policy row is no-op (future)
- Profile photo: use Supabase auth user metadata if available, fallback to initials
