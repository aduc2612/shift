# Phase 10 — RevenueCat Integration

## Objective
Integrate RevenueCat SDK for subscription-only access. Gate entire app behind "Shift AI Pro" entitlement. Paywall at end of onboarding. Customer Center in Settings.

## Context
- See `../BRIEF.md`
- See `../ROADMAP.md`
- Existing placeholder: `src/services/revenuecat.ts`
- Schedule screen: `src/app/(tabs)/index.tsx`
- Settings screen: `src/app/(tabs)/settings.tsx`
- Onboarding last screen: `src/app/(onboarding)/notif-permission.tsx` → navigates to `(tabs)`
- Reschedule hook: `src/features/schedule/hooks/useReschedule.ts`
- RevenueCat API key: `test_RjiJngvmmoReLpTJNcmunXnrJSf` (test/sandbox)
- Entitlement identifier: `default` (display name: "Shift AI Pro")
- Products: `yearly_sub`, `monthly_main`
- Design ref: `C:/Users/anhdu/Downloads/ord_settings (1).html` (Subscription section)

## Key Decisions
- **No free tier:** App requires active subscription. No free reschedule limit.
- **Paywall placement:** After onboarding (notif-permission screen), before entering (tabs).
- **Subscription guard:** `(tabs)/_layout.tsx` checks entitlement. Unsubscribed users → paywall screen.
- **Customer Center:** Settings → Account section, "Manage subscription" row.

## Tasks

### 1. Install dependencies
```
npx expo install react-native-purchases react-native-purchases-ui
```
- `react-native-purchases` — core SDK (configure, entitlements, customer info)
- `react-native-purchases-ui` — native paywall + customer center UI

### 2. RevenueCat service — TESTS FIRST
- Write `src/services/__tests__/revenuecat.test.ts`
  - `configureRevenueCat()` calls `Purchases.configure` with correct API key
  - `isSubscribed()` returns true when entitlement active
  - `isSubscribed()` returns false when no entitlement
  - `getCustomerInfo()` returns customer info
  - `getCustomerInfo()` handles errors gracefully
- Implement `src/services/revenuecat.ts`:
  ```ts
  import Purchases from 'react-native-purchases';
  
  const REVENUECAT_API_KEY = 'test_RjiJngvmmoReLpTJNcmunXnrJSf';
  const ENTITLEMENT_ID = 'default';
  
  export async function configureRevenueCat(): Promise<void> {
    Purchases.configure({ apiKey: REVENUECAT_API_KEY });
  }
  
  export async function isSubscribed(): Promise<boolean> {
    const info = await Purchases.getCustomerInfo();
    return ENTITLEMENT_ID in info.entitlements.active;
  }
  
  export async function getCustomerInfo() {
    return Purchases.getCustomerInfo();
  }
  ```

### 3. useSubscription hook — TESTS FIRST
- Write `src/hooks/__tests__/useSubscription.test.tsx`
  - returns loading initially
  - returns `isSubscribed: true` when entitlement active
  - returns `isSubscribed: false` when no entitlement
  - `refresh()` re-fetches customer info
  - handles errors gracefully
- Implement `src/hooks/useSubscription.ts`
  - Wraps RevenueCat customer info in a hook
  - Exposes: `{ isSubscribed, isLoading, refresh, customerInfo }`
  - Re-check on mount and on focus (app foreground)

### 4. Paywall screen
- Create `src/app/(paywall).tsx` — standalone screen outside (tabs)
  - Renders native RevenueCat Paywall via `react-native-purchases-ui`
  - On successful purchase → navigate to `/(tabs)`
  - On close (no purchase) → stays on paywall (user must subscribe)
- Update `src/app/(onboarding)/notif-permission.tsx`:
  - After onboarding complete → navigate to `/(paywall)` instead of `/(tabs)`

### 5. Subscription guard in tabs layout
- Update `src/app/(tabs)/_layout.tsx`:
  - Call `useSubscription()` hook
  - While loading → show spinner
  - If not subscribed → redirect to `/(paywall)`
  - If subscribed → render tabs normally

### 6. Paywall presentation helper — TESTS FIRST
- Write `src/services/__tests__/revenuecat.test.ts` (extend existing)
  - `presentPaywallIfNeeded()` returns true if already subscribed
  - `presentPaywallIfNeeded()` shows paywall and returns purchase result
- Implement `presentPaywallIfNeeded()` in `src/services/revenuecat.ts`
  - Uses `RevenueCatUI.presentPaywall()` from `react-native-purchases-ui`
  - Returns `{ purchased: boolean; customerInfo: CustomerInfo }`

### 7. Customer Center in Settings
- Update `src/app/(tabs)/settings.tsx`:
  - Add "Subscription" section between Account and AI Preferences
  - Rows:
    - "Current plan" — shows "Shift AI Pro" (or loading state)
    - "Renewal date" — from customer info entitlement expiration (if available)
    - "Manage subscription" — opens Customer Center via `presentCustomerCenter()`
  - Use `useSubscription()` hook for data
- Import `presentCustomerCenter` from `react-native-purchases-ui`

### 8. Entitlement check on reschedule
- Update `src/features/schedule/hooks/useReschedule.ts`:
  - Before calling AI reschedule, check `isSubscribed()`
  - If not subscribed → show paywall via `presentPaywallIfNeeded()`
  - If subscribed → proceed with reschedule
- Belt-and-suspenders: guard is also in tabs layout, but explicit check here prevents edge cases

### 9. Unit tests for all new code
- RevenueCat service tests (configure, entitlement check, customer info)
- useSubscription hook tests (loading, subscribed, unsubscribed, refresh)
- Reschedule entitlement gate tests
- Settings subscription section rendering tests

## Verification
- `npm test` — all new and existing tests pass
- App builds without errors (`npx expo run:ios` or `npx expo run:android`)
- New user flow: signup → onboarding → paywall → subscribe → enters app
- Existing subscriber flow: login → enters app directly (entitlement check passes)
- Settings → Subscription section shows current plan
- Settings → "Manage subscription" opens Customer Center
- Reschedule button works for subscribed users
- Unsubscribed user redirected to paywall on tab access

## Done When
- RevenueCat SDK configured with test API key
- Paywall appears after onboarding for new users
- Subscription guard blocks unsubscribed users from (tabs)
- Settings has Subscription section with Customer Center
- Reschedule gated behind entitlement check
- Unit tests written and passing
- No TypeScript errors

## Manual Steps (User Action Required)
- [ ] RevenueCat dashboard: verify entitlement `default` exists
- [ ] RevenueCat dashboard: verify products `yearly_sub` and `monthly_main` attached to offering
- [ ] RevenueCat dashboard: configure paywall design (visual editor in dashboard)
- [ ] iOS: enable In-App Purchase capability in Xcode
- [ ] Android: verify BILLING permission in AndroidManifest.xml
- [ ] Test purchases with sandbox/test accounts on device

## Notes
- API key starts with `test_` — sandbox mode. Swap for production key before release.
- `react-native-purchases-ui` handles all paywall rendering — no custom paywall UI needed.
- Customer Center is a native screen from RevenueCat — handles subscription management, cancellation, restore.
- RevenueCat entitlement `default` is the identifier; "Shift AI Pro" is display name only.
